#include<iostream>
#include<bits/stdc++.h> 
using namespace std;
int main(){
    // string str="sarthak";
    // sort(str.begin(),str.end());
    // cout<<str;
    // int nums[]={1,3,4,2,2};
    // int n=sizeof(nums)/sizeof(nums[0]);
    // vector<int> v(nums,nums+n);
    // sort(v.begin(),v.end());
    // for(int i=0;i<n;i++){
    // if(v[i]==v[i+1]) cout<< v[i]<<" ";
    // }
}